# Building Footprint + LiDAR Height Pipeline
# Install: pip install -r scripts/requirements_pipeline.txt

# Core geospatial
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
rasterio>=1.3.0

# LiDAR point cloud processing
pdal>=3.2.0          # PDAL Python bindings  (brew install pdal on macOS)
laspy>=2.5.0         # LAZ/LAS reader
laszip>=0.2.0        # Python binding for LASzip (LAZ decompression)

# Raster / array
numpy>=1.26.0
scipy>=1.11.0
rasterstats>=0.19.0  # Zonal statistics (building polygon → raster height)

# HTTP / data fetching
requests>=2.31.0
tqdm>=4.66.0         # Progress bars for large downloads

# Optional: faster GeoJSON parsing
orjson>=3.9.0
